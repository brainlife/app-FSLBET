#!/bin/bash
#PBS -l nodes=1:ppn=8,walltime=0:10:00
#PBS -N FSLBET

set -x

export input=$(jq -r .input config.json)
export fthresh=$(jq -r .fthreshold config.json)

singularity exec -e docker://brainlife/fsl:5.0.9 bet $input mask.nii.gz -m -f $fthresh

